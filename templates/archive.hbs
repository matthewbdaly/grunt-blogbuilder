{{> header }}

<div class="container">
    <div class="row">
        <div class="col-md-8">
{{#each posts}}
            <h4><a href="{{ this.path }}">{{ this.meta.title }}</a></h4>
{{/each}}
        </div>
        {{> sidebar }}
    </div>
</div>

{{> footer }}
