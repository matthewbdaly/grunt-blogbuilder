{{> header }}

<div class="container">
    <div class="row">
        <div class="col-md-8">
            <h1>Category: {{category}}</h1>
{{#each posts}}
            <h2><a href="{{ this.path }}">{{capitalize this.meta.title}}</a></h2>
{{/each}}
        </div>
        {{> sidebar }}
    </div>
</div>

{{> footer }}
